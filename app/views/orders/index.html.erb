<h1>My Orders</h1>

<% if @orders.any? %>
  <ul>
    <% @orders.each do |order| %>
      <li>
        <%= link_to "Order ##{order.id}", order_path(order) %>
        - Total Price: $<%= order.total_price %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No orders found.</p>
<% end %>
